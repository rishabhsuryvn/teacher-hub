<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="/global.css">
    <link rel="stylesheet" href="/search-result.css">

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    <title>Teacher-Hub</title>
</head>

<body>
    <div class="box nav">
        <h1>Teacher Hub</h1>
    </div>
    <div class="container">
        <div class="box">
            <% if (locals.data.length> 0) { %>
                <% data.forEach((data, index)=> { %>
                    <div id="header">
                        <div id="pic" class="">
                            <img src=<%=data.photo_path %> alt="">
                        </div>
                        <div id="teacher-details" class="ghost">
                            <table class="details-table">
                                <tr>
                                    <td>
                                        Name:
                                    </td>
                                    <td class="text-light">
                                        <%= data.full_name %>
                                    </td>
                                </tr>
                                <tr>
                                    <td>
                                        EID:
                                    </td>
                                    <td class="text-light">
                                        <%= data.eid %>
                                    </td>
                                </tr>
                                <tr>
                                    <td>
                                        Department:
                                    </td>
                                    <td class="text-light">
                                        <%= data.department %>
                                    </td>
                                </tr>
                                <tr>
                                    <td>
                                        Email:
                                    </td>
                                    <td class="text-light">
                                        <%= data.email %>
                                    </td>
                                </tr>
                            </table>
                        </div>
                    </div>
                    <div id="routine" class="ghost">
                        <img src=<%=data.routine_path %> alt="">
                    </div>
                    <% }) %>
                        <% } %>
                            <% if(locals.data.length==0){ %>
                                <h1 style="padding: 5rem;">NO DATA FOUND</h1>
                                <% } %>

        </div>
    </div>
</body>

</html>